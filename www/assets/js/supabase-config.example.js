// 이 파일을 supabase-config.js 로 복사한 뒤 값을 채워주세요.
// .gitignore 에 등록되어 있으므로 실제 키는 외부에 공유되지 않습니다.

window.SUPABASE_CONFIG = {
  url: 'https://your-project-id.supabase.co',
  anonKey: 'your-anon-public-key-here'
};
